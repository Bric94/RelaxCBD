{% extends 'base.html.twig' %}

{% block title %}Réinitialiser le mot de passe{% endblock %}

{% block body %}
<section class="login-container">
    <div class="login-box">
        <h1 class="login-title">Mot de passe oublié ?</h1>
        <p>Entrez votre adresse email pour recevoir un lien de réinitialisation.</p>

        {% for message in app.flashes('reset_password_error') %}
            <div class="alert-error">{{ message }}</div>
        {% endfor %}

        {% for message in app.flashes('success') %}
            <div class="alert-success">{{ message }}</div>
        {% endfor %}

        {{ form_start(requestForm, {'attr': {'class': 'login-form'}}) }}
            {{ form_label(requestForm.email, 'Email') }}
            {{ form_widget(requestForm.email, {'attr': {'class': 'form-input'}}) }}
            {{ form_errors(requestForm.email) }}

            <button class="btn-primary-auth" type="submit">📨 Envoyer le lien</button>
        {{ form_end(requestForm) }}
    </div>
</section>
{% endblock %}