{# templates/base.html.twig #}
<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>
			{% block title %}Relax CBD Shop
			{% endblock %}
		</title>
		<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🛒</text></svg>">
		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}
	</head>
	<body class="{% block body_class %}{% endblock %}">
		<div class="page-wrapper">
			<div class="background-parallax"></div>
			<nav class="navbar">
				<div class="container">
					<a class="logo" href="{{ path('home') }}">Relax CBD Shop</a>

					<ul class="nav-links">
						<li>
							<a href="{{ path('home') }}">Accueil</a>
						</li>
						<li>
							<a href="{{ path('product_index') }}">Produits</a>
						</li>
						<li>
							<a href="{{ path('cart_index') }}">Panier</a>
						</li>
						<li data-bp="800">
							<a href="{{ path('order_index') }}">Commandes</a>
						</li>
						<li data-bp="950">
							<a href="{{ path('app_contact') }}">Contact</a>
						</li>
						{% if app.user %}
							<li data-bp="1133">
								<a href="{{ path('user_index') }}">Mon compte</a>
							</li>
							<li data-bp="1133">
								<a href="{{ path('app_logout') }}">Déconnexion</a>
							</li>
						{% else %}
							<li data-bp="1133">
								<a href="{{ path('app_login') }}">Connexion</a>
							</li>
							<li data-bp="1133">
								<a href="{{ path('app_register') }}">Inscription</a>
							</li>
						{% endif %}

						<li class="nav-more">
							<button aria-label="Plus de liens">…Plus</button>
							<ul class="more-menu"></ul>
						</li>
					</ul>

					<button class="burger-menu" aria-label="Menu mobile">☰</button>
				</div>
			</nav>

			<aside id="mySidenav" class="sidenav" aria-hidden="true">
				<button class="closebtn" aria-label="Fermer le menu">&times;</button>
				>
				<a href="{{ path('home') }}">Accueil</a>
				<a href="{{ path('product_index') }}">Produits</a>
				<a href="{{ path('cart_index') }}">Panier</a>
				<a href="{{ path('order_index') }}">Commandes</a>
				<a href="{{ path('app_contact') }}">Contact</a>
				{% if app.user %}
					<a href="{{ path('user_index') }}">Mon compte</a>
					<a href="{{ path('app_logout') }}">Déconnexion</a>
				{% else %}
					<a href="{{ path('app_login') }}">Connexion</a>
					<a href="{{ path('app_register') }}">Inscription</a>
				{% endif %}
			</aside>
			<main class="main-content">
				{{ include('flash.html.twig') }}
				{% block body %}{% endblock %}
			</main>

			<div class="cart-popup" id="js-cart-popup">
				<div class="cart-popup__backdrop"></div>
				<div class="cart-popup__dialog">
					<p id="js-cart-popup-message"></p>
					<div class="cart-popup__actions">
						<button class="btn-secondary" id="js-cart-popup-continue">Continuer mes achats</button>
						<button class="btn-primary" id="js-cart-popup-view">Voir mon panier</button>
					</div>
				</div>
			</div>

			<footer class="custom-footer">
				<p>&copy; 2025 Relax CBD Shop - Tous droits réservés.</p>
				<a href="{{ path('app_contact') }}">Nous contacter</a>
			</footer>
		</div>
		{{ encore_entry_script_tags('app') }}
	</body>
</html>
